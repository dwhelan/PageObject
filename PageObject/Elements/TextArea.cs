using Coypu;

namespace PageObject.Elements
{
    public class TextArea : Text
    {
        public TextArea(ElementAttribute attribute, BrowserSession browser) : base(attribute, browser) { }
    }
}
